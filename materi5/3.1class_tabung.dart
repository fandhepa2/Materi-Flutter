class Tabung {
  // double vtabung = 0;
  // double ltabung = 0;
  vtabung(double p, r, t) {
    double v = p * r * r * t;
    return v;
  }

  ltabung(double p, r, t) {
    double v = p * r * r * t;
    return v;
  }
}
